{
  "name": "@priime-demo/subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "Verifiable ERC-7540 vault: NAV attested by a Priime operator quorum, indexed on Base.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "sync-factory": "./scripts/sync-factory.sh",
    "deploy": "graph deploy priime-demo --node https://api.studio.thegraph.com/deploy/ --version-label v0.0.2 --network-file ./networks.json --network base",
    "auth": "echo 'Run: graph auth <YOUR_STUDIO_DEPLOY_KEY>'"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "assemblyscript": "0.28.20"
  }
}
