{
  "name": "@priime-demo/loop-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Control-plane library for deploying user loops as workflows on the shared Priime service: service.json mutation, handler deploys, IPFS pinning, loop registry.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@priime-demo/journal-schema": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "ajv": "^8.17.1",
    "typescript": "5.5.4",
    "vitest": "^3.2.4"
  }
}
