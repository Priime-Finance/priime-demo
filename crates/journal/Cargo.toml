[package]
name = "priime-journal"
version = "0.1.0"
edition = "2021"
description = "Frozen v1 NAV-strike journal types (backend<->frontend seam). Mirrors schema/journal.v1.schema.json."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
